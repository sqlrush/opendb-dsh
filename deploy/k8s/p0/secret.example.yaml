# cp secret.example.yaml secret.yaml && fill DEEPSEEK_API_KEY (secret.yaml is git-ignored)
apiVersion: v1
kind: Secret
metadata:
  name: opendb-dsh-llm
  namespace: opendb-dsh
type: Opaque
stringData:
  DEEPSEEK_API_KEY: "sk-REPLACE_ME"
  # 可选：Kimi K3（2026-08-29）与智谱 GLM-5.3（2026-09-17 起默认模型）
  OPENDB_KIMI_API_KEY: "REPLACE_ME"
  OPENDB_GLM_API_KEY: "REPLACE_ME"
