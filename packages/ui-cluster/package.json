{
  "name": "@opendb-dsh/ui-cluster",
  "version": "0.0.1",
  "description": "k8s cluster status panel (client-only): architecture diagram with per-type coloured pods and call wires, node view, events, and a managed-database fleet matrix that scales to hundreds of nodes",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "exports": { ".": { "types": "./lib/types/index.d.ts", "default": "./lib/index.js" }, "./client": "./lib/client.js", "./package.json": "./package.json" },
  "dsh": { "client": { "platform": "web" } },
  "files": ["lib"],
  "scripts": { "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json && node build-client.mjs", "test": "echo no tests" },
  "peerDependencies": { "@deepseek-ai/cordis": "^4.0.1" },
  "devDependencies": { "@deepseek-ai/cordis": "^4.0.1", "@types/react": "^18.3.12", "esbuild": "^0.24.0", "react": "^18.3.1" }
}
